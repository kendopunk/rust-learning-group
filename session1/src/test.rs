#[cfg(test)]
mod test {
    /*
     UNCOMMENT HERE...
    // we're importing the functions from main.rs
    use crate::add_f64s;
    use crate::add_i8s;
    use crate::add_u32s;

    #[test]
    fn test_add_i8s() {
        let a: i8 = 1;
        let b: i8 = 2;
        assert_eq!(add_i8s(a, b), 3i8);
    }

    #[test]
    fn test_add_u32s() {
        let a: u32 = 10;
        let b: u32 = 20;
        assert_eq!(add_u32s(a, b), 30);
    }

    #[test]
    fn test_add_f64s() {
        let a = 1.2f64;
        let b = 2.8f64;
        assert_eq!(add_f64s(a, b), 4.0);
    }

    #[test]
    fn fix_this_tuple_test() {
        let tup: (i32, i32, f64) = (10, 20, 30.5);
        assert_eq!(tup.1, 20.0);
    }

    // Figure out a way to have the variables "second" and "third"
    // ignored by the compiler, i.e. get rid of the "unused variable" message
    // HINT:  The compiler will guide you
    #[test]
    fn fix_this_other_tuple_test() {
        let tup: (u8, u8, i32) = (1, 2, 300);
        let (first, second, third) = tup;
        assert_eq!(first, 2);
    }

    #[test]
    fn test_this_shorthand_array() {
        let arr = [10; 5];
        assert_eq!(arr.len(), 10, "The length of arr is");
    }
    ...TO HERE
    */
}
