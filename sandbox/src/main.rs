/**
 * Use this workspace as a sandbox for testing out code snippets --OR--
 * use Rust's online REPL <https://play.rust-lang.org/>
 *
 * To run from the command line (root of the project), use:
 * $> cargo run --bin sandbox
 */
fn main() {
    println!("Hello sandbox!");
}
